# Handoff — Post Phase 7.7 (2026-04-18)

Session handoff written at the end of the 2026-04-18 cycle that shipped:
- `5b53b2c` — Phase 7.6 (recommendation force fix, research follow-through, R4 async research)
- `88f6c76` — docs: mark 7.4 SHIPPED, unblock 7.1/7.3 specialist replication
- `a38d2ba` — Phase 7.7 (Ideation host for contentless cold opens)

Branch `main` was pushed clean at `a38d2ba`.

---

## Primary tasks for the next session

### 1. BUILD.md archive split — create `BUILD-ARCHIVE-1.md`

The planning doc is getting dense. Time to separate "living plan" from "historical reasoning."

**Target shape:**

- **`BUILD.md`** becomes the living plan: phase names, section headers, task checkboxes with brief one-line status, plus everything currently `[~]` or `[ ]` in full detail. Each completed section keeps a one-line summary and a link:
  > *"See [BUILD-ARCHIVE-1.md §Phase 7.6](BUILD-ARCHIVE-1.md#76-...) for design details."*

- **`BUILD-ARCHIVE-1.md`** gets the full original descriptions for every phase currently marked SHIPPED / complete.

**Completed sections to archive (in order):**

- Phase 0 (Foundation) — complete
- Phase 1 (Agent Configuration) — complete
- Phase 2 (LangGraph Orchestrator) — complete
- Phase 3 (Streaming and Client Integration) — complete
- Phase 4 (Institutional Memory and Idea Records) — complete
- Phase 5 — R1 (providers), R2 (accumulation), R5 (epistemics) shipped; R3 is `[~]`; R4 shipped
- Phase 6.2 (testing ladder)
- Phase 7.1 — Marketer-only shipped
- Phase 7.3 — Marketer-layer shipped
- Phase 7.4 — shipped
- Phase 7.5 — shipped
- Phase 7.6 — shipped
- Phase 7.7 — shipped

**Stays in `BUILD.md` main:**

- All `[ ]` and `[~]` items
- Phase 7.1/7.3 replication roadmap
- Phase 7.2 rollout (currently folded into the replication cycle)
- Phase 5 R3/R6/R7
- "Parked considerations" section (Product, Brainstorm, Champion)
- "Phase 7 Complete When" criteria
- "Principles That Apply to Every Phase" footer

**Pattern for each archived phase entry in the living `BUILD.md`:**

```markdown
### 7.6 Recommendation force / research follow-through / R4 async
**Status:** SHIPPED 2026-04-18 (commit 5b53b2c).
**Full detail:** [BUILD-ARCHIVE-1.md §7.6](BUILD-ARCHIVE-1.md#76-...)
- [x] Recommendation force — `force_recommendation` state flag
- [x] Research follow-through rule in `WORKER_RESEARCH_EPISTEMICS`
- [x] R4 async — `lib/research/scheduler.ts` + `async` flag on `RoutingDecisionSchema`
```

Intent: a reader scanning `BUILD.md` sees what's done + what's next. A reader wanting *why* a thing was designed a given way goes to the archive.

### 2. `*.md` audit — reference map

Grep `*.md` from repo root. Catalogue each file. Ensure the three canonical docs (`BUILD.md`, `CLAUDE.md`, `README.md`) point to the non-stale ones with a one-line description.

**Files likely to surface:**

- `docs/testing.md` — already referenced from CLAUDE.md and BUILD.md.
- `docs/full-result.example.json` — JSON, skip (not markdown).
- `relevant-zansei-materials/` — gitignored directory; the README you'll write (task 3 below) lives inside it and won't be tracked, but a short reference in CLAUDE.md explaining *"this directory exists locally for reference; it's not in the repo"* is worth having.
- `docs/handoffs/2026-04-18-post-7.7.md` — this file (new). Add to the reference map.
- Any other `*.md` files that have accumulated.

**Also fix while there:** CLAUDE.md line ~378 currently says `test/ ← gitignored` — that's stale. Actual `.gitignore` only ignores `/test/results/`. Personas and fixtures are tracked (see `test/personas/` in git).

### 3. `relevant-zansei-materials/README.md`

Gitignored, lives locally only. Purpose: reference port source for research patterns (R4 already ported, eventual R3 batching to come).

**Map:**

- Purpose statement — what this directory is, why it's local-only.
- File list with one-line descriptions: `research_orchestrator.py`, `conversation.py`, `tools/executor.py`, `tools/fetch.py`, `tools/search.py`, `test/results/` (if present).
- Which patterns we've already ported:
  - R4 async dispatch → `lib/research/scheduler.ts` (port of `in_flight` flag concept + merge accumulation).
  - Strategic brief synthesis → `recommendationNode` (assumption check, divergence rule, budget hierarchy).
- Which patterns still to port:
  - R3 batching — `BatchPlan` concept, `MAX_TOOL_ROUNDS_PER_BATCH = 4`, `MAX_TOTAL_TOOL_ROUNDS = 12`.
  - In-flight locking — Zansei's `_flight_lock` prevents duplicate batches; GetIdea's scheduler doesn't have this guard yet (acceptable at current volume, but a prerequisite for R3).
- **"Do not import or execute this code" rule** at the top in bold — it's Python, runs against a different stack, and the patterns matter more than the implementation.

---

## Out of scope this session

Code changes. Agent prompts. Graph or routing logic. Anything Product / Brainstorm / Champion-related. The async-default flip. Specialist replication.

This session is **documentation and structure only.** If you find a bug in the code while reading, flag it via `spawn_task` (or note it for a follow-up cycle) — don't fix it inline.

---

## Current repo state

- Branch `main` at `a38d2ba`, pushed to `origin/main`.
- Working tree clean.
- 11 active agents seeded in Supabase (Marketer, Finance, Creative, Copywriter, Designer, Accountant, Operations, Legal, CX, Realist, Ideation) + Orchestrator.
- Orchestrator prompt updated with Ideation cold-open routing + "When to defer research" async block.
- All tests green as of the last validation run:
  - `test:graph` 10/10
  - `test:grade` 3/3
  - `test:fixtures` 7/7
  - `npx tsc --noEmit` clean
- Three commits ahead of the prior session-start point (`8955276`): `5b53b2c`, `88f6c76`, `a38d2ba`.

---

## Sequencing for future cycles (after this doc work)

From the sequence agreed on during the 2026-04-18 session, what remains:

1. **This session** — `relevant-zansei-materials/README.md` + doc audit + BUILD.md archive split.
2. **Async-default flip + in-flight guard** — half-day cycle. Flip the orchestrator's bias from sync-by-default to async-by-default for enrichment research; add `in_flight` lock in `lib/research/scheduler.ts` (port from Zansei).
3. **Specialist replication** — the big quality unlock. 9 × (voice rewrite + case library + falsifiability via full batch). Multi-cycle.
4. **Parked considerations** (revisit each when warranted):
   - Potential **Product** specialist — own cycle if/when field evidence supports it.
   - **Brainstorm** register — likely a Creative prompt enrichment, not a new seat.
   - **Champion/Evangelist** as Realist's counterweight — only if the room proves too skeptical.

---

## Open judgment-call flags carried forward

From BUILD.md §7.6, §7.7, and §7.1 replication notes — these are explicit "we made a call, the next phase should watch for the following signal":

- **R4 async observability:** never observed end-to-end in a real persona transcript — orchestrator was conservative on the personas tested and didn't emit `async: true`. Scheduler proven correct by direct smoke test against Serper. A future cycle needs either (a) a persona that reliably triggers research (volunteers a URL early + asks a research-gated question) or (b) an orchestrator-prompt bias flip making async the default for enrichment fetches.

- **Ideation's soft-signal threshold:** the "route to Ideation on contentless openers" rule is prompt-level guidance, not a code check. Intentional — keeping orchestrator judgment intact is a Phase 7 principle (see memory: *Routing is the Art*). But the soft threshold needs observation across real user openers. A future phase should audit the ledger for:
  - How often Ideation fires on turn 1 (expected: pure greetings only).
  - How often it fires on turn 2+ (would be a bug — handoff is not working).
  - Whether a specialist is wrongly skipped because Ideation picked up an opener with actual signal.

- **Walter falsifiability framing:** run the full 12-persona batch and check whatever specialists naturally fire. Not 1:1 persona-to-specialist — real deliberations pull 3–4 specialists per conversation. Specialists whose archetype is rarely triggered by the existing pool (probably Legal, possibly Accountant) may warrant a targeted persona added later, but this is additive, not a prerequisite for specialist replication.

---

## Don't look for

- A handoff file from before this one. This is the first handoff file in the repo. Past sessions committed state via commit messages only.
- A `BUILD-ARCHIVE-*.md` file — it's being created in this session (task 1).
